# x402scan listing

Resource: `POST /v1/research` and MCP tool `research_mentions`.

Amount: `20000` atomic USDC (`$0.02`).
Network: `eip155:8453` (Base). Staging remains `eip155:84532` (Base Sepolia).

Origin: `https://mentionforge.mentionforge.workers.dev`

`.well-known/x402` is built from the request origin (works on `*.workers.dev` and custom domains).

Bazaar seed: run one successful paid MCP call after deploy (`npm run seed-bazaar`).
