# 2026-09-21

MentionForge **1.1.0** intelligence upgrade is in the Worker: agent-routed free tools (`get_example`, `suggest_tool`, `entity_profile`), paid lenses (`compare_brands`, `get_digest`, `detect_risk`, `draft_reply`) on the same $0.02 USDC / 10-call shared trial, vs-query fusion + longest-first SOV, compact/markdown overlays, Reddit Brave site-only fallback, query-gated GitHub/Stack Overflow inside web.

Price, Glama TDQS A invariants, and health `sources` (five platform ids) are unchanged. GitHub Discussions stay on `/search/issues` (no extra GraphQL inside the 8s budget).
